meta {
  name: Get All Todos
  type: http
  seq: 4
}

get {
  url: {{base_url}}/todos?open
  body: none
  auth: bearer
}

params:query {
  open: 
}

auth:bearer {
  token: {{access_token}}
}

assert {
  res.status: eq 200
}
